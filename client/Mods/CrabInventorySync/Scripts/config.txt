# CrabInventorySync v0.0.1 Configuration
# Lines starting with # are comments.

# Relay server URL. Only change if self-hosting.
# serverUrl = https://crab.dudiebug.net

# --- Sync toggles (set false to leave that category alone) ---
syncWeapon      = true
syncAbility     = true
syncMelee       = true
syncCrystals    = true
syncHealth      = true
syncWeaponMods  = true
syncAbilityMods = true
syncMeleeMods   = true
syncPerks       = true
syncRelics      = true
syncSlots       = true

# Internal PlayerState property name for crystals.
# If crystals always read as 0, use debug.lua (F7) to find the correct name.
crystalsProperty = Crystals
