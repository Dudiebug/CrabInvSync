# CrabInventorySync configuration
# Lines starting with # are comments.

# Relay server URL. Only change this if you are self-hosting the server.
# serverUrl = https://crab.dudiebug.net

# Room code — all players who want to sync must use the same value.
roomCode = default

# --- What to sync (set false to leave that item alone) ---
syncWeapon      = true
syncAbility     = true
syncMelee       = true
syncCrystals    = true
syncWeaponMods  = true
syncAbilityMods = true
syncMeleeMods   = true
syncPerks       = true
syncRelics      = true

# Health sync has a readiness gate in main.lua, but keep it disabled for the
# current solo inventory test build until health startup reads are verified in-game.
syncHealth      = false
syncSlots       = true

# Scalar item metadata writes are quarantined until stable slot identity is proven.
# The client still reads, sends, receives, merges, and compares {n,l,a,e}; it does
# not write InventoryInfo.Level, AccumulatedBuff, or Enhancements while this is false.
allowScalarMetadataApply = false

# Lifecycle and multiplayer safety gates. The client must pass several shallow
# stable probes before reading/pushing/applying. Joined clients are read-only by
# default, and multiplayer applies stay disabled until contribution/role safety is
# implemented and tested.
allowJoinedClientApply = false
allowMultiplayerApply = false
hostOnlyApply = true
lifecycleStableTicksRequired = 5

# Internal PlayerState property name for crystals.
# Health is not configured by property name: current HP is read from
# CrabHC.HealthInfo.CurrentHealth and max HP from CrabHC.HealthInfo.CurrentMaxHealth.
# When syncHealth is false, the client does not read, send, or apply health.
# Armor plates are intentionally not synced yet.
crystalsProperty = Crystals
