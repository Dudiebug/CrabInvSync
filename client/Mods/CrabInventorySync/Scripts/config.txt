# CrabInventorySync configuration
# Lines starting with # are comments.

# Relay server URL. Only change this if you are self-hosting the server.
# serverUrl = https://crab.dudiebug.net

# Room code — all players who want to sync must use the same value.
roomCode = default

# --- What to sync (set false to leave that item alone) ---
syncWeapon      = true
syncAbility     = true
syncMelee       = true
syncCrystals    = true
syncWeaponMods  = true
syncAbilityMods = true
syncMeleeMods   = true
syncPerks       = true
syncRelics      = true
syncHealth      = true
syncSlots       = true

# Internal PlayerState property name for crystals.
# Health is not configured by property name: current HP is read from
# CrabHC.HealthInfo.CurrentHealth and max HP from CrabHC.HealthInfo.CurrentMaxHealth.
# Armor plates are intentionally not synced yet.
crystalsProperty = Crystals
